import { isCourseInUnit } from 'front/helpers/is-course-in-unit';
import { module, test } from 'qunit';

module('Unit | Helper | is course in unit');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isCourseInUnit([42]);
  assert.ok(result);
});
