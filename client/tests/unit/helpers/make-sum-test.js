import { makeSum } from 'front/helpers/make-sum';
import { module, test } from 'qunit';

module('Unit | Helper | make sum');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = makeSum([42]);
  assert.ok(result);
});
