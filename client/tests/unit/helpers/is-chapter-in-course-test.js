import { isChapterInCourse } from 'front/helpers/is-chapter-in-course';
import { module, test } from 'qunit';

module('Unit | Helper | is chapter in course');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isChapterInCourse([42]);
  assert.ok(result);
});
