import { questionFileState } from 'front/helpers/question/file-state';
import { module, test } from 'qunit';

module('Unit | Helper | question/file state');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = questionFileState([42]);
  assert.ok(result);
});
