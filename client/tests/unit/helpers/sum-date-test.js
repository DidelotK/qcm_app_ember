import { sumDate } from 'front/helpers/sum-date';
import { module, test } from 'qunit';

module('Unit | Helper | sum date');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = sumDate([42]);
  assert.ok(result);
});
