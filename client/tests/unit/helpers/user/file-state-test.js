import { userFileState } from 'front/helpers/user/file-state';
import { module, test } from 'qunit';

module('Unit | Helper | user/file state');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = userFileState([42]);
  assert.ok(result);
});
